package keeper_test

import (
	"testing"
)

func TestGetParams(t *testing.T) {
	//k, ctx := testkeeper.BundlesKeeper(t)
	//params := types.DefaultParams()
	//
	//k.SetParams(ctx, params)
	//
	//require.EqualValues(t, params, k.GetParams(ctx))
}
